# This file contains example settings which should mostly work for own deployment. Of course you need to enter your own domain and access data.


# #############################################################################
# general settings (no action necessary)
# #############################################################################

# valid values are "DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL"
LOGLEVEL = "INFO"


# #############################################################################
# deployment settings which needs to be changed
# #############################################################################


remote = 'some_asteroid.uberspace.de'
user = 'some_user'


# #############################################################################
# deployment settings which should be OK for most use cases
# #############################################################################


app_name = "fair-debate"
PROJECT_NAME = 'fair_debate_web'
url_path = "/%(app_name)s"
port = '8000'

ALLOWED_HOSTS = ["%(app_name)s.%(user)s.uber.space"]
ALLOWED_HOSTS__DEVMODE = ["localhost"]

BASE_URL = "//%(app_name)s.%(user)s.uber.space/"
BASE_URL__DEVMODE = "//localhost:%(port)s"

deployment_path = "/home/%(user)s/%(PROJECT_NAME)s-deployment/%(app_name)s"


# needed to set the correct web backend; expexted to start with `/`
django_url_prefix = "/"
static_url_prefix = "/static"

# working directories during production (after successful deployment)
# `__BASEDIR__` will be replaced inside settings
STATIC_ROOT = "__BASEDIR__/collected_static"
MEDIA_ROOT = "__BASEDIR__/media"
REPO_HOST_DIR = "__BASEDIR__/content_repos"

DJANGO_LOGFILE = "__BASEDIR__/django_logfile.log"
BASE_APP_LOGFILE = "__BASEDIR__/base_app_logfile.log"



# this path sould be located outside <BASEDIR> because the deployment might delete that completely
BACKUP_PATH = "__BASEDIR__/../%(PROJECT_NAME)s_db_backups"

DB_FILE_NAME = "db.sqlite3"

# name (not path)
venv = "%(PROJECT_NAME)s-venv"

pip_command = "pip3.11"
python_version = "python3.11"


DEBUG = false
DEBUG__DEVMODE = true


# secret keys can be created with: python3 -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY = 'NfdysN_yXKWuliPb--example--W5_Fc9VUDOVnPgUBJ2oeq6FjAOJykFWFpK1PL7URw'

ADMIN_PASS = '9_muuozzBL--example-secret--IV70wX6YbRc_TkL3s'

[credentials]

admin_pass = "mr5iocfs4w--example-secret--reC8Ab8FAQPEPKIoQ"
test_user_1_pass = "ARmOXS_LVo--example-secret--wOcyAwoNQuAqCJAc4"
test_user_2_pass = "zcow9_LVVn--example-secret--nOvvOQEokh8zTseyo"
test_user_3_pass = "Ty1ism9zsy--example-secret--pTlc4KfxSV0avwLJs"
test_user_4_pass = "jiy9bqfQMB--example-secret--MOL5cQiqmlHQsHZEE"
test_user_5_pass = "fTuSvX-AD8--example-secret--pzxcYaXiwWalUveg0"
test_user_6_pass = "wJP0egaelj--example-secret--qrpRCeLWKUOC8Rml4"
test_user_7_pass = "Ipn5Aqea3r--example-secret--ejG2ggyVf0fRGOrD0"
test_user_8_pass = "ql0UK7MwOx--example-secret--KoA3Gwk7PU6YCqb3Q"
